{
  "name": "cloud-storage-api",
  "version": "1.0.0",
  "description": "Cloudflare Workers backend for cloud storage",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute cloud-storage --file=schema.sql"
  },
  "dependencies": {
    "@cloudflare/workers-types": "^4.20240605.0"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "wrangler": "^3.38.0"
  }
}
